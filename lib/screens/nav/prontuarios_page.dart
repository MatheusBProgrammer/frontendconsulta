// Definindo páginas para cada seção
import 'package:flutter/material.dart';

class ProntuariosPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Página de Prontuários'),
    );
  }
}
